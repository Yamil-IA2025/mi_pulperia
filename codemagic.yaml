workflows:
  default-workflow:
    name: Default Workflow
    scripts:
      - name: Setup
        script: |
          flutter clean
          flutter pub get
          # SIN build_runner - el archivo ya está en git
          
      - name: Build Android
        script: flutter build apk --release
        
    artifacts:
      - build/app/outputs/flutter-apk/*.apk