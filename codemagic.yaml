workflows:
  default-workflow:
    name: Default Workflow
    scripts:
      - name: Setup
        script: |
          flutter clean
          rm -f pubspec.lock
          flutter pub get
          
      - name: Build Android
        script: flutter build apk --release
        
    artifacts:
      - build/app/outputs/flutter-apk/*.apk